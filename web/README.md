# EdgeKite Web Dashboard

Frontend prototype generated by Google AI Studio (GAIS).

## Origin

This code was generated by GAIS based on our prompt in `docs/GAIS-PROMPT.md`.
The upstream repo is [edge-kite-gais](https://github.com/llm-case-studies/edge-kite-gais).

**Note:** GAIS can only push (one-way). To get updates from GAIS, merge from their repo.

## Status

This is a **frontend prototype** with mock data. It needs to be wired to the real EdgeKite API.

### What's Real
- UI/UX architecture and components
- Theming engine (Edge/Ranch/Legal/Fun-EE)
- Telemetry tracker (`services/tracker.ts`)
- i18n structure (EN, ES, PT, DE)

### What's Simulated
- Event stream (generated in `mock-data.ts`)
- System metrics (CPU, RAM, DB size)
- Persistence (localStorage, not SQLite)

## Running Locally

```bash
cd web
npm install
npm run dev
```

Requires `GEMINI_API_KEY` in `.env.local` for AI features (Privacy Airlock).

## Integration TODO

- [ ] Replace `mock-data.ts` with real `/api/events` calls
- [ ] Connect to EdgeKite SSE stream (`/api/stream`)
- [ ] Persist settings to EdgeKite config API
- [ ] Build and bundle for embedding in Rust agent

## Tech Stack

- React 18 + TypeScript
- Vite
- Tailwind CSS (via CDN in index.html)
- Recharts (charts)

## See Also

- [BACKEND_HANDOFF.md](../docs/BACKEND_HANDOFF.md) - API contracts
- [GATEWAY_STRATEGY.md](../docs/GATEWAY_STRATEGY.md) - Theme system rationale
